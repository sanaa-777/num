{
  "name": "dalil-yemen-static-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -c tailwind.config.cjs -i ./tailwind.input.css -o ./app/tailwind.css --minify",
    "stamp:build": "node scripts/stamp-build.js",
    "build": "npm run build:css && npm run stamp:build",
    "serve": "python3 -m http.server 4173"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "firebase-tools": "^15.25.1"
  }
}
